# Protocol

Stage 1 does not implement the weather packet protocol yet.

Planned modules:

- packet assembler for BLE fragments
- protocol parser for versioned binary packets
- ingress router for snapshot and position updates
- ACK generation for accepted packets
